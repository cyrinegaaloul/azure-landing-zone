# Jelastic P4D Deployment Notes

This folder documents the application shape expected by a future Jelastic P4D deployment.

## Planned Inputs

- container image: `landing-zone-demo-app:latest`
- port: `8080`
- health endpoint: `/health`
- metrics endpoint: `/metrics`

## Why This Is A Placeholder

Jelastic P4D deployment details depend on the target platform account, registry integration, and environment topology. This repository prepares the app now so the final deployment wiring can be completed shortly before the demo.
