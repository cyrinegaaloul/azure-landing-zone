apiVersion: v1
kind: Service
metadata:
  name: landing-zone-demo-app
  namespace: demo
spec:
  selector:
    app: landing-zone-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
